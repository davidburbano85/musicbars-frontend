services:
  - type: static_site
    name: musicbars
    buildCommand: npm install && npm run build
    publishDir: dist/musicbares-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html